export interface EnvInterface {
  API_PREFIX: string;
  PORT: number;
  DB_DIALECT: string;
  DB_URL: string;
  DB_PORT: number;
  DB_USER: string;
  DB_PASS: string;
  DB_NAME: string;
  JWT_SECRET_KEY: string;
  JWT_EXPIRES_IN: number;
}
